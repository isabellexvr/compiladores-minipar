# Programa cliente servidor de uma calculadora aritmética simples
# Cliente solicita operação, servidor calcula e devolve
c_channel calculadora computador_1 computador_2

SEQ
  # Simulação de interação (placeholder: valores fixos)
  operacao = 1; # suponha 1=+
  valor1 = 7;
  valor2 = 5;
  resultado = 0;
  calculadora.send(operacao, valor1, valor2, resultado);
  # Cliente imprimirá depois de receber (simplificado)
  calculadora.receive(operacao, valor1, valor2, resultado);
  print resultado;
